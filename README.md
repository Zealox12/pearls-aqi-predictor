Most of the information is in the docx report, but to notify relevant files, fetch_meteo is for feature selection and data detching, 
train_meteo is for training models, predict_meteo is for predicting, ed_meteo is the jupyter notebook with eda using open meteo, all other similar files
without meteo next to them are old files which used open weather.
